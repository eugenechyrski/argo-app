apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: helm-gogs
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: argocd
    server: {{ .Values.spec.destination.server }}
  project: default
  source:
    path: .
    repoURL: https://github.com/keyporttech/gogs-helm-chart
    targetRevision: main
    helm:
      values: |
        config:
          secretKey: "secret key"
          disableInstaller: true
          offlineMode: false
          requireSignin: false
          disableRegistration: true


